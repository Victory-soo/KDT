<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link rel="stylesheet" href="./css/Post.css">
  <script defer src="./js/test.js"></script>
</head>

<body>
  <form action="/posts" method="POST">
    <div>
      <label>제목</label>
      <input type="text" name="title">
    </div>
    <div>
      <label>내용</label>
      <input type="text" name="content">
    </div>

    <button class="submit" type="submit">제출</button>
  </form>

  <h1 class="titleList">글 목록</h1>
  <h2 class="total">총 글 수 : <%= postCounts %>개</h2>
  <ul>
    <% if(postCounts > 0) { %>
    <% for(let i = 0; i < postCounts; i++) {%>
    <li>
      <p class="title title1"> 제목 : <%= ARTICLE[i].title%></p>
      <p class="title title2"> 내용 : <%= ARTICLE[i].content%></p>
      <a href="#" onclick="deleteArticle('<%= ARTICLE[i].title %>')">글 삭제</a>

    </li>
    <% } %>
    <% } else { %>
    <li>글 정보가 없습니다.</li>
    <% } %>
  </ul>
</body>

<script>
  function deleteArticle(title) {
    fetch(`http://localhost:4000/posts/${title}`, {
      method: 'delete',
      headers: {
        'Content-type': 'application/json'
      },
    }).then((res) => {
      location.reload();
    })
  }
</script>

</html>