// @ts-check

// alert('!!');

const button = document.querySelector('.admit');
console.log(button);

button.addEventListener('click', () => {
  const putTitle = prompt('제목을 입력하시오.');
  const putContent = prompt('내용을 입력하시오.');

  throw putTitle;
  throw putContent;
});
